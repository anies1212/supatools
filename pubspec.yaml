name: supatools_workspace
description: Monorepo workspace for Supabase code generation tools
version: 0.0.0
publish_to: none

environment:
  sdk: ^3.5.0

workspace:
  - packages/supabase_schema_core
  - packages/supafreeze
  - packages/suparepo

dev_dependencies:
  melos: ^7.3.0
